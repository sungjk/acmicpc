language: java
jdk:
  - openjdk8
before_install:
  - chmod +x gradlew
  - chmod +x gradle/wrapper/gradle-wrapper.jar
sudo: true
script:
  - ./gradlew clean test
notifications:
  email:
    recipients:
      - ajax0615@gmail.com
    on_failure: always
